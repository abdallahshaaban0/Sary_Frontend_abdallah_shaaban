export const environment = {
  production: true,
  apiURL:"http://localhost:3000/"
   /*how to run this localhost through cmd `json-server --watch db.json`
  for more info visit https://www.npmjs.com/package/json-server */
};
